# 小世界-摘要

**版本**：v2.0
**状态**：遗迹优先设计基线
**最后更新**：2026 年 8 月 14 日
**当前实现**：阶段 1 步骤 1-6 已完成；地图、Runtime、独立时钟、WorldView、共享异步寻路与固定 tick 移动已经接通，正式残破古修道场可以生成 `48x48` 地貌和五个原版 Building
**当前唯一交付对象**：遗迹
**后续用途**：宗门驻地、完整独立世界
**统一生命周期边界**：全部小世界状态只存在于当前游戏会话，不写入或恢复世界存档

## 一句话定义

小世界是 Cultiway 自己持有地图、实体、时间和模拟状态的独立世界容器。它不是第二个原版 `MapBox`，也不只是主世界入口旁的一段抽象计时；玩家能够在主世界地图周边直接观察其中真实发生的移动、探索、战斗、交互和世界演化。

小世界最终服务三类用途，但当前不同时实现三套玩法：

| 用途 | 世界主体 | 与主世界的关系 | 核心玩法 | 当前定位 |
| --- | --- | --- | --- | --- |
| 遗迹 | 进入的修士 Projection、遗构、守卫、危险与机缘 | 一处主世界遗迹绑定一个共享 Runtime | 发现、进入、调查、冒险、取得机缘、撤离 | **当前唯一实现目标** |
| 宗门驻地 | 宗门成员、访客、建筑、资源与防御设施 | 由宗门拥有，通过入口往返 | 建设、生产、驻守、来访与攻防 | 遗迹发布后另立规划 |
| 完整独立世界 | 小世界原生生物、聚落、势力与文明 | 可有入口，也可作为纯观察世界存在 | 生态、人口、文明和事件自主演化 | 宗门驻地之后另立规划 |

三类用途只共享确实相同的世界容器能力。遗迹目标、宗门生产和文明模拟不得塞入一个按 `SubWorldKind` 分支的巨型 Runtime。

## 已确认的产品决策

| 决策 | 结论 | 设计影响 |
| --- | --- | --- |
| 遗迹操控 | 修士由 AI 自主探索 | 正式玩法不提供逐格移动、调查、领取或攻击指令；玩家负责观察、聚焦和时间控制 |
| 遗迹实例关系 | 同一处主世界遗迹共享一个 Runtime | 遗构调查、残阵变化、危险、守卫、机缘耗尽和地形变化会被后续队伍继承；每次远征不得重建副本 |
| 完整独立世界 | 长期目标包含原生人口、聚落和文明演化 | 未来必须支持不依赖主世界 Actor 的原生实体，但这不是遗迹前置能力 |
| 状态持久性 | 全部只属于当前游戏会话 | 不设计存档 DTO、恢复、迁移和跨会话稳定 ID；切换世界或重新载入时统一销毁 |

会话边界只约束 Site、Runtime、Binding、参与者知识和未结算战利品。修士已经安全返回后，物化到主世界的物品、生命变化和其他正式结果就是普通主世界状态，继续遵循项目现有存档行为。

## 当前问题与路线修正

现有代码已经完成阶段 1 步骤 1-6，但仍只是开发中的正式场景底座：

- 主世界与小世界已经共用同一个异步 `PathFinder` worker 池；小世界具备八向禁切角搜索和固定 tick 移动，移动实体允许重叠，但当前移动实体仍是测试 Pawn。
- Runtime 直接维护移动目标、未来路线 handle、已认领移动段和 terrain Navigation；请求真实状态由共享 `PathFinder` 的 `PathPollKind` 给出，不在实体上复制状态枚举。重新指定目标只替换当前段终点之后的路线，不重置连续 `Position`。
- 正式残破古修道场已经具备开放地貌和五个 Building，但尚没有真实遗迹入口、Actor Projection、调查、机缘、危险、奖励或返回结算。
- 现有后续文档把六类物品、四类制造、完整技能重构和多实例调度放在首个遗迹之前，导致平台越来越宽，玩法闭环仍不存在。

新路线从阶段 1 步骤 7 继续纵向交付：先让一名真实修士进入并自主调查当前手工开放遗迹，再按实际玩法需要增加调查状态、机缘、危险、战斗、战利品和多实例稳定性。

## 当前范围

### 必须完成

1. 保持已完成的共享 Navigation、寻路和固定 tick 连续位移，并用正式 Actor/Building 内容覆盖其合同。
2. 建立一处主世界遗迹入口与一个开放的残破古修道场 Runtime。
3. 让一名真实 Actor 以最小 Projection 进入，小世界期间暂停其主世界推进。
4. 让 AI 只依据已知 Tile、Building 和 EvidenceObservation 自主调查多个独立机缘槽，并自行决定继续深入或带着所得返回。
5. 把伤势和至少一种真实机缘收益同步结算回主世界。
6. 同一遗迹 Runtime 的 Building/Tile 物理变化和机缘槽耗尽状态对后续进入者持续生效，但私有 Knowledge 不自动继承。
7. 在后续阶段加入确定性开放地貌组合、遗构与线索分布、守卫战斗、死亡、两类真实战利品和多队伍争夺。
8. 保证相机聚焦、视图裁剪和主世界倍速不进入小世界逻辑输入；回放相同 path publication trace 时结果一致。
9. 在切换世界、返回主菜单、重新加载世界或 Mod 重载时释放全部 Runtime、Binding、WorldView 和用途层索引。

### 当前明确不做

- 第二个原版 `MapBox`、`WorldTile`、Actor manager、Building manager、zone 或原版寻路器。
- 遗迹内炼丹、炼器、制符、卷轴制作、工作台和制造进程转移。
- 一次性覆盖全部 SpecialItem、装备、消耗品和技能载体。
- 为小世界复制一套 SkillLibV3、伤害公式、terrain 定义或物品定义。
- 通用规则模块市场、任意目标 DAG、脚本热加载和自由地图编辑器。
- BodyTime、梦境时间、独立昼夜和大量 VisualProfile。
- 未经实测就固定 Active/Warm、8 实例、1 ms 或其他平台级容量结论。
- 宗门建设、资源生产、原生生态、王国与文明演化。
- 存档、恢复、跨会话状态或兼容旧版本数据。
- 正式玩法中的玩家逐格操控；右键移动只保留为 Debug 验证工具。

## 职责边界

### 小世界 Core

`Source/Core/SubWorlds` 只负责不同用途确实共有的能力：

- `SubWorldTemplateAsset`、Generator、ClockProfile 与 VisualProfile 资产链。
- `SubWorldMapData`、`SubWorldTile[]`、`SubWorldGrid` 和原版 terrain Asset 解析。
- `SubWorldRuntime` 的 Store、SystemRoot、Clock、命令队列和 Revision。
- `SubWorldManager` 的创建、查询、更新、命令、销毁与会话清理。
- Runtime 内局部坐标、terrain Navigation、移动和确定性固定 tick。
- `SubWorldActor`、`SubWorldBuilding` 的通用类别、`Position`、共享 Visual、由原版 `BuildingAsset.fundament` 派生的空间索引、Runtime-local `LocalObjectId` 分配与统一生命周期。
- WorldView 所需的只读状态与用途无关的 Actor/Building 视觉事件。

Core 不持有主世界 `Building`、主世界 `Actor`、`Sect`、遗迹能力/目标、宗门库存或文明数据；它只为用途层提供小世界内通用 Actor/Building/Tile 容器能力。

### 主世界边界适配

远征边界负责真实 Actor 与 Projection 的进入和返回：

- 以当前会话内 `SourceActorId` 建立唯一 Binding。
- 捕获首个遗迹实际需要的只读快照。
- 用内存索引阻止主世界 AI、移动、受伤、修炼和渲染继续推进该 Actor。
- 在退出边界应用明确的生命、资源、奖励或死亡结果。
- 不把原版 Actor、`ActorBinder` 或主世界 Entity 放入 Runtime Store。
- `SettlementStagingRegistry`、`ISubWorldSettlementCommitPort`、`DeathApplicationLedger` 与 death port 属于主世界边界；Runtime 只看到 Token、StagingId 和幂等结果，不持有 Actor、holder 或目标 Entity。

### 遗迹用途层

`Source/Content/SubWorlds/Ruins` 负责：

- `RuinSiteId -> InstanceId` 的当前会话绑定。
- 主世界入口、发现状态、进入资格和远征队伍。
- 遗迹地貌、Actor、Building、Tile 环境、非空间 MetaObject，以及遗构上的地标、证据源、危险控制器和机缘能力。
- 自主调查 AI、参与者/公开知识、机缘槽领取、战利品归属和遗迹逐步耗尽/崩塌。
- 将 Runtime 事件转成入口状态、WorldLog 和最终奖励。

`SubWorldManager` 不应知道“遗迹”是什么；遗迹用途层也不能绕过命令和事件边界直接改写 Runtime 内部集合。

### 后续用途层

- 宗门驻地以当前会话内 Sect ID 绑定 Runtime，拥有自己的建设、生产和驻守系统。
- 完整独立世界拥有小世界原生人口、生态和文明系统，不以主世界 Actor Projection 作为人口主体。
- 二者可以复用 Grid、Runtime、Navigation、View 和必要的访客边界，但不得复用遗迹的目标状态机或战利品规则来假装完成自身玩法。

## 小世界对象模型

小世界只承认四类独立存在形态，内容能力不能自行升级为第五类对象：

| 类别 | 运行表示 | 生命周期与空间语义 |
| --- | --- | --- |
| Actor | Friflo Entity | 能移动、感知、决策或战斗的空间对象；Projection、守卫和未来原生单位都属于 Actor |
| Building | Friflo Entity | 拥有 Anchor/Footprint、可观察或交互的静态空间对象；残碑、药圃、阵眼、道台、尸骸和地面掉落都属于 Building |
| Tile | row-major `SubWorldTile[]` 与 Runtime 环境层 | 稠密 terrain、阻挡和实际环境场；Tile 不创建 Entity |
| MetaObject | 用途 Service 持有的非空间聚合记录 | 拥有成员、归属、公开知识或聚合生命周期；`RuinSiteRecord` 是首个 MetaObject，后续队伍、宗门和势力按真实需求加入 |

当前一处遗迹只绑定一个 Runtime，Site membership 可由 `RuinSiteRecord.InstanceId` 和 Runtime 反向用途句柄直接表达；在第二种真正需要同类聚合的用途出现前，不建立通用 MetaObject manager 或任意成员图。

Actor 和 Building 通过组件组合实际已经实现的能力，不建立继承树。Core 只提供类别、Position、原版 fundament 空间索引、生命周期和 View 所需的通用能力；`Landmark`、`EvidenceSource`、`HazardController`、`Opportunity` 等概念只有在遗迹用途层出现对应可运行系统时才建立，不能为了规划中的未来步骤提前进入代码。

当前步骤的物理对象只有普通 Actor、Building 和 Tile。残碑、药圃、阵眼、道台等先以原版 Building 表达；调查、危险或机缘在对应 System 真正实现时再决定所需组件，不提前建立能力 ID、仓储或状态机。纯 terrain/残墙属于 Tile；只有拥有独立 Building 表现和原版 fundament 的对象才创建 Building。

遗迹信息严格分为三类权威来源：

| 信息 | 示例 | 所有者 |
| --- | --- | --- |
| 物理事实 | Building 位置、阵眼是否稳定、机缘槽是否耗尽、Tile 是否危险 | Runtime 的 Building 组件与 Tile 环境层 |
| 观察者知识 | 看过的格子、已知遗构、方向证据、失败调查、已知危险 | Participant/Party Knowledge；明确公开的事实复制到 Site MetaObject 的 Public Knowledge |
| 派生视图 | 未探索/部分探索/耗尽、已知地标数量、活动危险数量 | 从物理事实与允许公开的 Knowledge 生成的只读 ReadModel |

`RuinSiteReadModel` 必须显式接收 `Public`、`Participant(ParticipantId)` 或仅 Debug 可用的 `Debug` disclosure context。Known 字段按对应 Knowledge 过滤；`IsExhausted`、物理生命周期和 Admission 可用性始终从全部物理事实派生，不因观察者不同而改变。正式 AI/UI 不能请求 Debug context。

`Unknown/LeadKnown/Located/Attempted` 属于观察者知识；`Available/Reserved/Exhausted` 属于机缘槽物理状态；`Active/Stabilized/Destroyed` 属于危险控制器物理状态。禁止再用一个 `Unrevealed -> Clued -> Exposed -> Claimed -> Exhausted` 枚举混合认知与物理生命周期，也不保存可从真实组件和 claim 派生的第二份进度计数。

本阶段不建立 `conditions[] + effects[]`、字符串 handler、通用 FeatureSlot 解释器或目标 DAG。古修道场由强类型 Generator 创建固定的 Building 能力组合，由具体 System 处理调查、危险和领取；第二种真实内容出现后，只提取两边已经重复且语义一致的能力。

## 身份模型

| 身份 | 所有者 | 作用域 | 规则 |
| --- | --- | --- | --- |
| `InstanceId` | `SubWorldManager` | 当前会话 | Runtime 技术身份；不等于遗迹、宗门或世界的业务身份 |
| `RuinSiteId` | 遗迹用途层 | 当前会话 | 一处主世界遗迹的业务身份；一对一绑定当前 Runtime |
| `ParticipantId` | 远征边界 | Runtime 生命周期 | 一次进入关系；同一 Actor 同时只能有一个 |
| `SourceActorId` | 主世界 | 当前主世界 | 退出时找到真实 Actor；不得用于 Runtime 热路径访问 Actor |
| `LocalObjectId` | `SubWorldRuntime` | 当前 Runtime | Runtime-local 物理 Building 身份；Generator placement 和未来运行时对象从同一个不复用的分配器取得，用于空间索引与后续实际需要的命令 |
| `OpportunityClaimKey` | 遗迹用途层 | 当前 Runtime | `(SourceLocalObjectId, SlotIndex)`；定位某个 Building 上的机缘槽，不创建 Opportunity 实体或 `OpportunityId` |
| `RuinLootId` | 遗迹用途层 | 当前 Runtime | 一份可携带、遗落和跨 Store 物化的真实战利品身份；阶段 3 首次因死亡遗落需要时建立，阶段 4 扩展完整 typed loot，不等于来源 Building 或 claim key |
| `SubWorldAnchor` | 当前用途层 | 当前会话 | 现有 Create 必需的主世界创建上下文；对遗迹表示入口位置，但不等于业务身份或所有权 |
| `SlotId` | `SubWorldSpatialLayout` | View 生命周期 | 只决定画在主地图周边哪里，不参与玩法与随机结果 |

切换世界或重新加载时这些身份全部失效。入口 Building 不得把旧 `InstanceId` 当作可恢复数据；发现孤立入口时由遗迹用途层删除或失效处理，不创建兼容回退 Runtime。未来完整独立世界若不需要主世界入口，应在其规划中把 Owner、Anchor 和 View Host 解耦；不能把当前遗迹 Anchor 或周边 Slot 当成独立世界身份，也不为此阻塞遗迹阶段。

## 核心运行结构

`SubWorldRuntime` 继续直接拥有运行状态：

```text
InstanceId / TemplateId / Seed / State / Revision
MapData / Grid / NavigationContext / PathWorldKey
EntityStore / SystemRoot
Clock
BoundaryCommandQueue / MoveCommandQueue
```

`SubWorldGeneratedScene` 直接包含 MapData、命名出生点、`SubWorldActorPlacement[]` 和 `SubWorldBuildingPlacement[]`。Runtime 创建自己的 EntityStore 后统一物化这些场景对象；Generator 不持有 Runtime，也不存在用途 Bootstrap 或无类型生成 Plan。MetaObject 等用途状态只在实际 Service 出现后由该 Service 管理。

每个固定 tick 的顺序固定为：

1. 接收并验证边界命令。
2. 更新感知与参与者已知信息。
3. 自主 AI 选择一个当前行动。
4. 处理路径请求与排队的 Tile/Building 结构变化。
5. 推进移动并结算 Tile 环境效果。
6. 阶段 3 起处理攻击、命中、伤害与死亡；逻辑 projectile 只是 Combat System 持有的 typed in-flight record，不是第五类空间 Entity。
7. 对仍存活且未被伤害中断的 Actor 推进 Building 调查和其他交互。
8. 评估机缘槽 claim、战利品和退出资格。
9. 评估 Site 生命周期并按明确 disclosure context 生成只读视图状态。

相同 Seed、相同初始状态和相同不可变寻路请求快照必须得到相同 Tile 序列；异步 worker 在哪个 local tick 发布结果不属于生产确定性承诺，因此真实 worker 调度不同可能改变等待期间发生的移动危险、战斗和 claim 竞逐。逐 tick 或最终玩法结果复现必须额外记录并回放同一 path publication trace；不能只给 Seed 就宣称整局 bitwise deterministic。`ViewVisible`、聚焦目标和 Renderer 开关不得成为逻辑输入，但 LocalSpeed/worker 负载可以通过 publication trace 改变时序，测试必须明确属于哪一层保证。

## 地图与 terrain

当前正确设计保持不变：

- 地图使用 row-major `SubWorldTile[]`，`index = y * Width + x`。
- `SubWorldTile` 只保存 `MainAssetId/TopAssetId`，不是 terrain 定义副本。
- Runtime 创建时由 `SubWorldGrid` 直接解析原版 `TileType/TopTileType`。
- 有效 terrain 固定为 `Top ?? Main`；通行、移动成本、格伤害、图层和视觉读取同一对象。
- 不创建 `WorldTile`，不修改 `MapBox.width/height`、主世界 tile 数组或原版 Asset。
- 地图变化必须通过一个 Runtime 结构变更边界调用 `Grid.SetTile`、更新动态导航并递增 Revision；不得让三个操作分散在调用方。
- 路径只保存 tile index，不保存 `WorldTile` 或 WorldView 坐标。

## 首个遗迹体验

首个正式模板是 `Cultiway.SubWorld.Template.RuinedDaoGround`，使用一张 `48x48` 连续开放地图。它表现一处残破古修道场，而不是由房间和走廊组成的地牢：

| 地貌/遗构 | 作用 | 是否强制经过 |
| --- | --- | --- |
| 山脚裂隙与残阶 | 入口、撤离点和第一批模糊道痕 | 只有出入必经 |
| 倾塌外院遗构 | 提供遮挡、岔路和可调查残碑 | 否 |
| 荒废药圃 | 一处可独立取得的 Ingredient 机缘 | 否 |
| 断裂阵带 | 持续危险区；可绕行，也可调查阵眼降低危险 | 否 |
| 山腰废殿与道台 | 提供较强线索和另一处悟道/遗物机缘 | 否 |

地貌与 Building 锚点由手工布局保证可读性；Seed 只改变局部坍塌通路、Building 放置/能力 payload、危险强度、感知难度和秘密 EvidenceRelation。某个宿主是否 Located/Observed 只由各观察者 Knowledge 决定。地图不存在唯一“正确路线”，也不存在必须清完才能离开的核心宝箱。

首个闭环为：

1. 玩家通过正式世界神力投放古修道场入口，而不是依赖开发自动创建 Runtime。
2. 一名满足条件的修士由主世界 AI 发现并前往入口。
3. 到达后建立 Binding，在同一主线程边界创建 Projection。
4. Projection 观察地貌、灵气异常和道痕，只依据已知信息选择调查方向。
5. Projection 可以先调查残碑获得线索，也可以绕过断阵直接搜索药圃或道台。
6. 每个机缘都是某个 Building 上的独立槽；参与者可以先取得方向证据并定位它，物理槽只在领取时进入 Reserved/Exhausted，取得一个槽不会改变其他 Building。
7. Projection 根据风险偏好、生命状态和当前所得，自行决定继续调查还是返回。
8. 返回时结算伤势与实际取得的机缘；后续修士进入同一 Runtime，继承已稳定残阵、已耗尽槽和其他物理变化，但不会自动继承上一人的私有 Knowledge，仍可寻找剩余内容。

首阶段没有战斗和死亡。危险区最多把 Projection 生命降到 1；战斗、生死和掉落在阶段 3 一次性补齐，避免出现两套临时死亡语义。

## 遗迹状态

### Site MetaObject

Site 的知识、物理生命周期和探索摘要是三个正交维度：

```text
主世界公开知识：Hidden -> Discovered
物理生命周期：  Open -> Collapsing -> Collapsed
派生探索摘要：  Unexplored / Partial / Exhausted
```

- `Hidden/Discovered` 属于 Site MetaObject 的 Public Knowledge，不由相机是否看到入口决定，也不写入 Runtime Building。
- `Open` 表示物理 Site 可以接纳合格参与者；`Collapsing/Collapsed` 是不可逆的真实生命周期。
- `Exhausted` 从全部有限 Opportunity 槽、活动 claim 和崩塌状态派生，所有 context 一致；`Unexplored/Partial` 的展示细节按 disclosure context 的 Known 信息派生。两者都不保存为第二份可写进度。`IsExhausted` 首次为 true 时 Site 停止接纳新远征并可发布一次事件。
- 入口 Tooltip 可以组合公开知识、当前参与者关系和派生摘要，但不能反向修改任何一层。

每个 Opportunity 能力槽只保存物理状态：

```text
Available -> Reserved -> Exhausted
                \-> Available
```

`Reserved -> Available` 表示领取者放弃或结算回滚。Slot 只保存物理状态、payload 引用和 `ExhaustReason?`，不复制 owner；唯一 claim repository 按 `OpportunityClaimKey` 保存 `Participant(ParticipantId)` 或 `Loot(RuinLootId)` owner。非物理 Outcome 结算前由 Participant 持有；真实 loot 物化时 claim 原子转给 RuinLootId，拾取/遗落只改变 loot carrier，不改变来源 claim。结算或永久毁坏时槽进入 Exhausted、写明 Settled/Destroyed 原因并清除 claim。

方向证据、是否定位、是否尝试过等信息由各自 Knowledge 记录；同一个槽可以对甲是 Located、对乙仍是 Unknown，而其物理状态始终只有一份。一个槽 Exhausted 不改变其他 Building 的能力槽。

### Runtime 与单次远征

单个参与者退出不等于 Runtime 结束。遗迹是否仍值得探索由 Site ReadModel 派生；只取得部分机缘时 Runtime 继续 Running，保留剩余 Building、Tile 环境和能力状态供后续队伍探索。整处遗迹结束时，Site 先保持 Collapsing，完成所有参与者/claim/loot 结算，捕获不可变最终公开快照并销毁 Runtime，最后才提交生命周期 Collapsed。Provider 可以保留只含最终快照的 Site MetaObject/主世界坍塌入口供观察，也可以随后调用 DestroySite 一并移除；Collapsed Site 不再持有 Runtime。

单次远征状态为：

```text
Approaching -> Entering -> Exploring -> Returning -> Settling -> Settled
                                     \-> Dead（阶段 3）
                                     \-> Aborted
```

状态转换只发生在主线程边界或 Runtime tick 边界。任何失败都必须在调用返回前恢复为一个完整状态，不留下“Actor 已冻结但 Projection 不存在”或“奖励两边各一份”的中间态。

## 自主探索 AI

正式遗迹不接受玩家移动或交互命令。每个 Projection 只从以下信息决策：

- 自己和队友已经观察过的地形与遗构。
- 已感知的灵气异常、方向证据、危险覆盖、守卫，以及 Knowledge 中已经定位的 Building/Opportunity 槽。
- 当前生命、资源、已取得收益、返程成本和可到达路径。
- 自身风险偏好与调查能力；不得读取未定位 Building、隐藏能力 payload 或 Generator 的完整布局。

首批行动优先级：

1. 生命低于撤离阈值，或当前所得达到自身满意阈值时返回入口。
2. 阶段 3 起先处理已感知且必须应对的威胁。
3. 邻接具备 EvidenceSource、HazardController 或 Opportunity 交互的已知 Building 时执行对应调查/领取。
4. 在 Knowledge 已定位且物理状态可领取的机缘槽中，按收益、风险、路程和自身相性选择一个。
5. 没有已定位机缘时，沿最可信的 EvidenceObservation 前往对应方向与距离范围调查。
6. 没有证据时前往最近的观察边界或显著 Landmark Building。
7. 连续无法得到新信息或没有安全路径时返回入口。

AI 可以带着部分所得主动结束远征；这不是失败。它不因 WorldView 被裁剪而获得额外信息、改变风险判断或停止思考。

## Projection 与返回

阶段 1 只捕获闭环实际使用的数据：

```text
SourceActorId
DisplayName / ActorAssetId / VisualVariant
Position / MoveSpeed / PerceptionRange
CurrentHealth / MaxHealth
CurrentWakan / MaxWakan
ExplorationRiskPreference
```

不捕获完整 `ActorData`、任意 ECS 组件包、全部状态、装备或制造进程。主世界携带物在首阶段保持原状且不能在遗迹内使用；Projection 的战斗快照和实际战利品在需要它们的阶段再增加。

Binding 期间通过当前会话内索引统一阻止主世界侧 AI、移动、攻击、受伤、修炼、恢复和 Cultiway 定时 System。禁止通过写入将被存档的“永久隐藏”字段表达 Binding。若玩家保存后重新加载，加载出的世界只保留入场前的主世界 Actor 状态，小世界及未结算成果全部消失，符合会话边界。

退出结算使用 Runtime Prepare/Commit/Rollback 命令和一个窄的主世界提交端口：

1. `PrepareSettlementCommand` 在 Runtime tick 边界校验参与者、`OpportunityClaimKey` owner 与 Revision，冻结 Actor Projection 和对应 claim，并发布带 Token 的不可变结算快照。
2. `SubWorldExpeditionService` 只把 `SourceActorId/HolderId`、typed Outcome/Loot values、仅含标量/不可变 typed value 的 ActorOldValues、`MaterializerKey` 与纯数据参数写入会话内 `SettlementStagingRegistry`；禁止保存 Actor、Entity、Store、holder adapter、delegate、handler 或 Unity 对象。
3. 服务提交 `CommitSettlementCommand(Token, StagingId)`。Runtime 在 tick 边界预校验全部状态变化后，进入同一主线程的不可重入提交段，并调用窄接口 `ISubWorldSettlementCommitPort.TryPromote(StagingId)`。
4. MainWorld port 在该调用内创建/验证目标 Entity、绑定 holder、应用 Actor 字段并返回可逆 Receipt；随后 Runtime 执行无分配、无外部回调的机缘槽、claim、ledger 和 Projection 状态提交。
5. MainWorld promote 或 Runtime 最终提交任一步失败时，port 在退出提交段前按 Receipt 删除新 Entity、恢复 Actor 旧值，Runtime 同步恢复 Prepared 状态；不会留下跨 tick 可见的主世界半成品。
6. 两边成功后 Runtime 发布 CommittedEvent，服务移除 Binding 和 StagingRecord；Commit 之前的取消、入口删除或清理则删除 StagingRecord 并提交 `RollbackSettlementCommand(Token)`。
7. 阶段 3 起，Health 为 0 的参与者改走 DeadPendingSettlement/DeathSettlementToken/Acknowledge；`DeathApplicationLedger` 按 Token 幂等，死亡入口已生效后即使抛错也记 Applied，重复 Token 不再调用；不可判定的 Faulted 状态禁止重试并阻断发布。

保存、`clearWorld`、Mod 重载和普通事件派发都不能插入不可重入提交段；它们只能在提交前中止 staging，或在完整提交后观察正式主世界结果。用途层从不直接改 Building 能力状态、claim、ledger、Projection 或 Runtime loot。

阶段 1 的奖励只支持一种现有 Ingredient。阶段 4 再为实际使用的奖励类型建立 typed 值与跨 Store 转移；不提前实现六类通用 PortableItem。

## 视图与 UI

当前“主地图周边稳定槽位 + 共用主相机”方案继续作为所有会话内小世界的展示宿主：

- Runtime 局部坐标与槽位世界坐标分离；`SlotId` 不参与玩法身份。
- 同一相机视野可以同时相交主世界和多个 WorldView。
- 离开视野只停止 Renderer 和视觉同步，不改变 Runtime 时钟与逻辑。
- WORLD 神力分区保留主世界/实例入口、局部暂停、1x/2x/4x 和聚焦参与者控件。
- 正式遗迹按钮只负责聚焦和显示状态，不负责创建、重置或领取奖励。
- 点击实体可打开已有信息/Tooltip 语义；不新增大型遗迹操作窗口。
- 所有状态文本本地化；图标按钮复用原版 `PowerButton`、`UiIcons` 和 Tooltip。
- 右键移动、强制交互和生成测试 Pawn 只在 Debug 配置出现。

入口世界 Overlay 至少区分 Hidden、Open、内部有人、部分探索、Exhausted 与 Collapsed；残碑线索、危险道痕、可取得机缘和安全撤离使用不同视觉，不用同一个闪光效果表达所有状态。

## 时间与调度

- 每个 Runtime 继续使用现有 `1/24s` 固定 tick 与独立 LocalSpeed。
- 首版只保留 LocalTime，不引入 BodyTime、梦境比例或独立年历。
- 主世界和小世界可以分别暂停/变速；相机聚焦不改变任一速度。
- Runtime 在 `FramePriorityGovernor` 预算内推进；预算不足时保留 accumulator，不跳过逻辑时间。
- 遗迹没有参与者且没有倒计时事件时可以不执行 gameplay System，但 Runtime 和物理状态仍常驻。
- 多实例容量与每帧预算在阶段 5 用真实遗迹负载测量后确定，不在设计阶段写死。

## 三类用途的能力差异

| 能力 | 遗迹 | 宗门驻地 | 完整独立世界 |
| --- | --- | --- | --- |
| 地图/Grid/Navigation/View | 需要 | 复用 | 复用 |
| 主世界访客 Projection | 核心 | 访客与部分成员可用 | 可选访客 |
| 小世界原生 Actor/Building | 守卫 Actor 与遗构 Building | 驻地设施 Building、召唤 Actor 等 | 核心人口 Actor 与设施 Building |
| 独立机缘与逐步耗尽 | 核心 | 不使用遗迹机缘模型 | 不使用遗迹机缘模型 |
| 建设与生产 | 不做 | 核心 | 文明系统自行决定 |
| 生态与文明 | 不做 | 不做完整文明 | 核心 |
| 所有权 | 遗迹 Site | Sect | 独立世界自身 |
| 生命周期 | 机缘逐步耗尽或崩塌后结束 | 当前会话内长期存在 | 当前会话内长期存在 |
| 存档恢复 | 不做 | 不做 | 不做 |

宗门驻地开始前，遗迹阶段只需证明同一 Runtime 能容纳访客/守卫 Actor、用途 Building、Tile 环境和 Site/Party MetaObject；不需要提前实现驻地建筑能力或文明人口系统。

### 宗门驻地产品轮廓

宗门驻地是一处由 Sect 拥有、在当前会话内长期运作的小世界领地。其最小完整循环应是：宗门开辟驻地 -> 指派成员常驻 -> 建造/维护设施 -> 消耗与产出真实宗门资源 -> 接待访客或抵御入侵 -> 宗门或入口消失时有序撤离。

它在遗迹底座上新增：

- `SectId -> InstanceId` 所有权和宗门权限，而不是 `RuinSiteId`。
- 长期 Resident 关系；访客可继续用短期 Projection，但常驻成员不能用“远征 Returning/Settled”状态假装居住。
- 建筑放置、施工、耐久、占地和通行变化。
- 驻地库存、生产任务、维护消耗和明确资源流。
- 宗门成员调度、来访、攻防以及宗门解散/入口摧毁后的清退。

它不使用 Opportunity、Exhausted、遗迹线索、战利品 claim 或 Collapse 作为日常主循环。遗迹阶段也不提前建立这些驻地系统。

### 完整独立世界产品轮廓

完整独立世界是当前会话中真正自主演化的另一片世界，不以主世界访客为人口主体。其最小完整循环应是：创建地貌与资源 -> 生成原生生物/人口 -> 生存、繁衍和迁徙 -> 形成聚落与势力 -> 发生冲突、交流和兴衰 -> 玩家持续观察。

它在共享底座上新增：

- 没有 `SourceActorId` 的 NativeEntity 生命周期与繁衍。
- 本地资源、生态、人口、聚落、势力和文明时间推进。
- 小世界内部的长期空间所有权、建设、战争和事件。
- 可选的主世界入口与访客适配；没有入口时仍能被创建、模拟和观察。
- Owner、`SubWorldAnchor` 与 WorldView Host 解耦，不能把遗迹 Building 或周边 Slot 当作世界本体身份。

它不使用遗迹的逐步耗尽目标，也不能靠复用原版第二个 `MapBox` 假装完成。只有遗迹发布后已经证明 Grid、Runtime、Navigation、Actor/Building/Tile/MetaObject 四类存在与多实例调度可靠，才开始设计原生文明；否则会再次得到一个只有平台名词、没有可玩世界的空计划。

## 阶段导航

| 阶段 | 文档 | 目标 | 完成标志 |
| --- | --- | --- | --- |
| 1 | [遗迹纵向切片](小世界/第1阶段-遗迹纵向切片.md) | 在已完成的移动底座上加入开放古修道场、单 Actor Projection、最小正式神力 Provider、多个机缘槽与返回 | 不用开发自动创建，真实修士能自主取得部分机缘后返回，后续修士仍可探索剩余槽 |
| 2 | [遗迹生成与探索](小世界/第2阶段-遗迹生成与探索.md) | 古修道场具体配置、Seed 地貌变化、调查记忆、共享物理状态与开放遗址内容合同 | 至少 20 个固定 Seed 的所有机缘槽都独立可达，AI 不读取隐藏布局，后续队伍继承物理变化 |
| 3 | [遗迹战斗与生死](小世界/第3阶段-遗迹战斗与生死.md) | 最小共享基础战斗、两类守卫、撤退、死亡、失败与崩塌 | 成功、撤退、死亡、崩塌四条结算路径完整且可复现 |
| 4 | [遗迹战利品与争夺](小世界/第4阶段-遗迹战利品与争夺.md) | 4A typed 战利品、4B 多队竞争、4C 远古战场与两种内容后的最小 Archetype 抽取 | 三道闸门分别验收；同一物品不可复制，竞争由实际行动决定 |
| 5 | [遗迹接入与发布](小世界/第5阶段-遗迹接入与发布.md) | 扩展既有 Provider/Admission，加入 Hidden 发现、可选内容 Provider、组队、UI/日志、多实例测量与发布验收 | 玩家无需 Debug 工具可持续观察遗迹生态，50 次创建/结算/清理无残留 |

阶段 5 完成后才分别建立“宗门驻地”和“完整独立世界”摘要与阶段文档。当前不为这两类用途编写看似详细、实际没有消费者验证的 TODO。

## 资源总览

| 资源族 | 首次阶段 | 原则 |
| --- | ---: | --- |
| 原版 terrain Tile/Sprite | 已完成 | 继续直接复用 `TileTypeBase.sprites.main`，不复制基础 terrain |
| 入口、残碑、药圃、断阵、废殿与道台 | 1 | 优先核对 `.GameSource/Assets`；缺失时只新增用途专属遗构/Overlay sprite |
| Projection 外观 | 1 | 复用 Source Actor 的 ActorAsset 动画与比例，不复用主世界 renderer 实例 |
| 未探索遮蔽、道痕线索和危险反馈 | 2 | WorldView 私有材质/Overlay，不修改主世界全局 shader |
| 守卫与战斗反馈 | 3 | 守卫需要 idle/move/attack/hit/death；技能表现只绑定已支持能力 |
| 战利品地面表现 | 4 | 复用现有 ItemShape/ItemIconData；只补缺失的世界空间比例版本 |
| Site 状态与日志图标 | 5 | 复用原版 PowerButton、Tooltip 与语义状态，不创建独立大型窗口 |

## 最终完成标准

遗迹路线完成时，一处主世界遗迹对应一个当前会话共享 Runtime。修士能够由 AI 前往并进入连续开放的遗址，根据已观察地貌、遗构和道痕寻找多个独立机缘，在危险区与守卫之间权衡继续深入或带着部分所得返回。已调查遗构、已稳定残阵、死亡守卫、地形变化和已耗尽机缘由后续进入者继承；战利品只能由实际取得者带回且不会复制。

整个流程必须可由玩家在主相机下观察，但观察、聚焦和裁剪不改变结果。主世界 Actor 在 Binding 期间没有并发推进，退出后的伤势、奖励或死亡通过 Runtime Token 与主线程 Prepare/Commit/Rollback/Acknowledge 边界结算。切换世界或重新加载时全部小世界状态直接销毁，不恢复、不迁移、不保留孤立入口。完成这一产品后，宗门驻地和完整独立世界再分别复用已被遗迹验证的世界容器能力。